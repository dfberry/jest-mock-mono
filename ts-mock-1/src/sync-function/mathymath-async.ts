//import { add } from './mathymath';

// const mock = (fn: any, timeout = 1000) => {
//   return new Promise((resolve, reject) => {
//     setTimeout(() => {
//       const result = fn();
//       if (result) {
//         resolve(result);
//       } else {
//         reject({ message: 'Error' });
//       }
//     }, timeout);
//   });
// };

// export const addAsync = async () => {
//   try {
//     await mock(add, 1000);
//   } catch (e: any) {
//     console.log(e?.message as string);
//   }
// };
